# Remove NaN from Distance Matrix

It iteratively removes the taxon (row and column) with the largest number of NaNs, until there are no NaNs remaining.  That should ensure the fewest rows are removed to eliminate the NaNs.
